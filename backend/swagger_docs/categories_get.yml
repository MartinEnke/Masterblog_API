tags:
  - Categories
summary: Get all blog post categories
description: Returns a list of all categories currently used by blog posts.
responses:
  '200':
    description: List of categories
    content:
      application/json:
        schema:
          type: array
          items:
            type: string
          example: ["Technology", "Science", "Philosophy", "Travel"]
